Act as a Senior Machine Learning Engineer.

Using the cleaned dataset, prepare the data for machine learning and build a baseline fraud detection model.

Tasks:

* Create stratified train, validation, and test splits
* Scale numerical features appropriately
* Compare three imbalance strategies:

  1. Class Weights
  2. SMOTE Oversampling
  3. Random Undersampling

Train a Logistic Regression baseline model for each strategy.

Evaluate using:

* Precision
* Recall
* F1 Score
* ROC-AUC
* PR-AUC

Do not use Accuracy as the primary metric due to severe class imbalance.

Compare all approaches and identify the best baseline.

Provide visualizations and explain trade-offs.

Finish with recommendations for the next modeling phase.
